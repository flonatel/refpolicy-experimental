#!/bin/sh
set -e

. /usr/share/debconf/confmodule
db_version 2.0
db_input low 'refpolicy/reload' || true
db_go || true

# dh_installdebconf will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
